image: python2.7
# command to install dependencies
services:
  - mysql
  - postgres
script:
  - pip install Django==1.6.2 --use-mirrors
  - pip install coveralls --use-mirrors
  - python setup.py develop
  - coverage run example/manage.py test -v2
notify:
  email:
    recipients:
      - mjschultz@gmail.com
